# loan-prediction-streamlit
End-to-End Loan Prediction using ML + Streamlit
# Loan Prediction — End-to-End (Streamlit)

Simple end-to-end loan approval prediction demo with:
- Data preprocessing pipeline
- RandomForest classifier
- Model saving (joblib)
- Streamlit UI for interactive predictions



1. Clone repo  
2. Put dataset CSV in `data/loan_data.csv` (suggested datasets: Kaggle / Analytics Vidhya). :contentReference[oaicite:1]{index=1}  
3. Create virtualenv and install requirements:
   ```bash
   python -m venv venv
   source venv/bin/activate   # or venv\Scripts\activate on Windows
   pip install -r requirements.txt
